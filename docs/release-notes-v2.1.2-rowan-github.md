# [2.1.2] Rowan: Garden beds & weather Neo

**Codename:** Rowan · Garden layout customization + Weather Neo companion update

**Tag:** *(commit only — no GitHub release yet)*

## Summary
- Drag-and-drop reorder for habit cards in the garden
- Custom “beds” (грядки) — UI grouping independent of habit categories, via context menu
- Weather plugin **2.4.0**: Weather Neo visuals (opt-in), richer gradients, time-of-day tint, in-card CSS animations, hourly + 7-day sheet, compact Apple-like layout

## Notes
- Weather Neo is a plugin setting (`neoMode`); `minAppVersion` stays **1.7.0**
- Low-power mode (`data-low-power`) reduces Neo particles / disables storm flash
